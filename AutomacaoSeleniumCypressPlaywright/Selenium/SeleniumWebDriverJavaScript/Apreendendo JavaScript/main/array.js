// Se quiser, posso te mostrar também:
// estrutura ideal de projeto Node + Selenium
// como rodar testes com npm run test
// diferença entre npm start, npm run, npx e node (isso evita muita confusão).

const paises = ['Brasil', 'Argentina', 'Uruguai'] // lista de dados
console.log(paises) // Apresenta todos os dados
console.log(paises[1]) // Apresenta o dado do índice [0] = Brasil
console.log(paises.length) // Identificar tamanho do Array

// Operações com o Array
paises.push('USA') // Adiciona dado no final do Array
paises.pop() // Remove o último dado do Array
paises.unshift('Canadá') // Adiciona dado no começo do Array
paises.shift() // Remove o primeiro dado do Array

console.log(paises)