// Se quiser, posso te mostrar também:
// estrutura ideal de projeto Node + Selenium
// como rodar testes com npm run test
// diferença entre npm start, npm run, npx e node (isso evita muita confusão).

// Ele pode ser alterado
var nome = 'Rodrigo'
nome = 'Costa'
console.log(nome)

// Ele pode ser alterado
let valor = 17
valor = 22

// Não pode ser alterado
const cidade = 'VG'
const pais = ['BR', 'AG', 'USE']

// Boolean - Verdadeiro ou Falso
let maior_idade = false
let menor_idade = true

//Objeto
let devices = {
    nome: 'Iphone 14',
    preco: 8000,
    cores: {
        cor1: 'Verde',
        cor2: 'Amarelo'
    }
}

// Função